export type JsonUser = {
    userId: number;
    id: number;
    title: string;
    body: string;
};

export type JsonPost = {
    userId: number;
    id: number;
    title: string;
    body: string;
};
