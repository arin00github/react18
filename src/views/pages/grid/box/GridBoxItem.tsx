import React from "react";

import styled from "styled-components";

export const GridBoxItem = (): JSX.Element => {
    return (
        <div>
            <div>GridBoxItem</div>
        </div>
    );
};
